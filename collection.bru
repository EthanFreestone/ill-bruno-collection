script:pre-request {
  const {
    login, 
    loginWithExpiry, 
    setBaseUrl, 
    getBaseUrl,
    getFolioAxios
  } = require('@k-int/bruno-shared-scripts');
  setBaseUrl();
  
  //await login();
  
  await loginWithExpiry();
  const folioAxios = await getFolioAxios();
  
  const { v4: uuidV4 } = require('uuid');
  
  bru.setVar('uniqueUUID', uuidV4())
}
