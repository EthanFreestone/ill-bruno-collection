meta {
  name: POST Validate Directory Entry
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/ill/directory/entry/validate
  body: json
  auth: inherit
}

body:json {
  {
    "name": "validateTest",
    "type": "consortium",
    "slug": "valTest"
  }
}
